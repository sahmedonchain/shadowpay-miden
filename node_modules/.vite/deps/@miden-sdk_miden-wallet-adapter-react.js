import {
  require_jsx_runtime
} from "./chunk-A3XUOO42.js";
import {
  AllowedPrivateData,
  BaseMessageSignerWalletAdapter,
  PrivateDataPermission,
  WalletAdapterNetwork,
  WalletConnectionError,
  WalletDisconnectionError,
  WalletNotConnectedError,
  WalletNotReadyError,
  WalletNotSelectedError,
  WalletReadyState,
  WalletTransactionError,
  b64ToU8,
  scopePollingDetectionStrategy
} from "./chunk-OX5AHI45.js";
import {
  require_react
} from "./chunk-7GY2265W.js";
import {
  __toESM
} from "./chunk-5WRI5ZAA.js";

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/WalletProvider.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/useLocalStorage.js
var import_react = __toESM(require_react(), 1);
function useLocalStorage(key, defaultState) {
  const state = (0, import_react.useState)(() => {
    try {
      const value2 = localStorage.getItem(key);
      if (value2)
        return JSON.parse(value2);
    } catch (error) {
      if (typeof window !== "undefined") {
        console.error(error);
      }
    }
    return defaultState;
  });
  const value = state[0];
  const isFirstRender = (0, import_react.useRef)(true);
  (0, import_react.useEffect)(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    try {
      if (value === null) {
        localStorage.removeItem(key);
      } else {
        localStorage.setItem(key, JSON.stringify(value));
      }
    } catch (error) {
      if (typeof window !== "undefined") {
        console.error(error);
      }
    }
  }, [value, key]);
  return state;
}

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/useWallet.js
var import_react2 = __toESM(require_react(), 1);
var EMPTY_ARRAY = [];
var DEFAULT_CONTEXT = {
  autoConnect: false,
  connecting: false,
  connected: false,
  disconnecting: false,
  select(_name) {
    console.error(constructMissingProviderErrorMessage("get", "select"));
  },
  connect(_privateDataPermission, _network, _allowedPrivateData) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "connect")));
  },
  disconnect() {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "disconnect")));
  },
  requestTransaction(_transaction) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestTransaction")));
  },
  requestAssets() {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestAssets")));
  },
  requestPrivateNotes(_noteFilterType, _noteIds) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestPrivateNotes")));
  },
  signBytes(_message, _kind) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "signBytes")));
  },
  importPrivateNote(_note) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "importPrivateNote")));
  },
  requestConsumableNotes() {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestConsumableNotes")));
  },
  waitForTransaction(_txId, _timeout) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "waitForTransaction")));
  },
  requestSend(_transaction) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestSend")));
  },
  requestConsume(_transaction) {
    return Promise.reject(console.error(constructMissingProviderErrorMessage("get", "requestConsume")));
  }
};
Object.defineProperty(DEFAULT_CONTEXT, "wallets", {
  get() {
    console.error(constructMissingProviderErrorMessage("read", "wallets"));
    return EMPTY_ARRAY;
  }
});
Object.defineProperty(DEFAULT_CONTEXT, "wallet", {
  get() {
    console.error(constructMissingProviderErrorMessage("read", "wallet"));
    return null;
  }
});
Object.defineProperty(DEFAULT_CONTEXT, "address", {
  get() {
    console.error(constructMissingProviderErrorMessage("read", "address"));
    return null;
  }
});
Object.defineProperty(DEFAULT_CONTEXT, "publicKey", {
  get() {
    console.error(constructMissingProviderErrorMessage("read", "publicKey"));
    return null;
  }
});
function constructMissingProviderErrorMessage(action, valueName) {
  return `You have tried to  ${action} "${valueName}" on a WalletContext without providing one. Make sure to render a WalletProvider as an ancestor of the component that uses WalletContext`;
}
var WalletContext = (0, import_react2.createContext)(DEFAULT_CONTEXT);
function useWallet() {
  return (0, import_react2.useContext)(WalletContext);
}

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/WalletProvider.js
var initialState = {
  wallet: null,
  adapter: null,
  address: null,
  publicKey: null,
  connected: false
};
var WalletProvider = ({ children, wallets: adapters, autoConnect = false, privateDataPermission = PrivateDataPermission.UponRequest, network = WalletAdapterNetwork.Testnet, onError, localStorageKey = "walletName", allowedPrivateData = AllowedPrivateData.None }) => {
  const [name, setName] = useLocalStorage(localStorageKey, null);
  const [{ wallet, adapter, address, publicKey, connected }, setState] = (0, import_react3.useState)(initialState);
  const readyState = adapter?.readyState || WalletReadyState.Unsupported;
  const [connecting, setConnecting] = (0, import_react3.useState)(false);
  const [disconnecting, setDisconnecting] = (0, import_react3.useState)(false);
  const isConnecting = (0, import_react3.useRef)(false);
  const isDisconnecting = (0, import_react3.useRef)(false);
  const isUnloading = (0, import_react3.useRef)(false);
  const [wallets, setWallets] = (0, import_react3.useState)(() => adapters.map((adapter2) => ({
    adapter: adapter2,
    readyState: adapter2.readyState
  })));
  (0, import_react3.useEffect)(() => {
    setWallets((wallets2) => adapters.map((adapter2, index) => {
      const wallet2 = wallets2[index];
      return wallet2 && wallet2.adapter === adapter2 && wallet2.readyState === adapter2.readyState ? wallet2 : {
        adapter: adapter2,
        readyState: adapter2.readyState
      };
    }));
    function handleReadyStateChange(readyState2) {
      setWallets((prevWallets) => {
        const index = prevWallets.findIndex(({ adapter: adapter3 }) => adapter3 === this);
        if (index === -1)
          return prevWallets;
        const { adapter: adapter2 } = prevWallets[index];
        return [
          ...prevWallets.slice(0, index),
          { adapter: adapter2, readyState: readyState2 },
          ...prevWallets.slice(index + 1)
        ];
      });
    }
    adapters.forEach((adapter2) => adapter2.on("readyStateChange", handleReadyStateChange, adapter2));
    return () => adapters.forEach((adapter2) => adapter2.off("readyStateChange", handleReadyStateChange, adapter2));
  }, [adapters]);
  (0, import_react3.useEffect)(() => {
    const wallet2 = name && wallets.find(({ adapter: adapter2 }) => adapter2.name === name);
    if (wallet2) {
      setState({
        wallet: wallet2,
        adapter: wallet2.adapter,
        connected: wallet2.adapter.connected,
        address: wallet2.adapter.address,
        publicKey: wallet2.adapter.publicKey
      });
    } else {
      setState(initialState);
    }
  }, [name, wallets]);
  (0, import_react3.useEffect)(() => {
    function listener() {
      isUnloading.current = true;
    }
    window.addEventListener("beforeunload", listener);
    return () => window.removeEventListener("beforeunload", listener);
  }, [isUnloading]);
  const handleConnect = (0, import_react3.useCallback)(() => {
    if (!adapter)
      return;
    setState((state) => ({
      ...state,
      connected: adapter.connected,
      address: adapter.address,
      publicKey: adapter.publicKey
    }));
  }, [adapter]);
  const handleDisconnect = (0, import_react3.useCallback)(() => {
    if (!isUnloading.current)
      setName(null);
  }, [isUnloading, setName]);
  const handleError = (0, import_react3.useCallback)((error) => {
    if (!isUnloading.current)
      (onError || console.error)(error);
    return error;
  }, [isUnloading, onError]);
  (0, import_react3.useEffect)(() => {
    if (adapter) {
      adapter.on("connect", handleConnect);
      adapter.on("disconnect", handleDisconnect);
      adapter.on("error", handleError);
      return () => {
        adapter.off("connect", handleConnect);
        adapter.off("disconnect", handleDisconnect);
        adapter.off("error", handleError);
      };
    }
  }, [adapter, handleConnect, handleDisconnect, handleError]);
  (0, import_react3.useEffect)(() => {
    return () => {
      adapter?.disconnect();
    };
  }, [adapter]);
  (0, import_react3.useEffect)(() => {
    if (isConnecting.current || connected || !autoConnect || !adapter || !(readyState === WalletReadyState.Installed || readyState === WalletReadyState.Loadable))
      return;
    (async function() {
      isConnecting.current = true;
      setConnecting(true);
      try {
        await adapter.connect(privateDataPermission, network, allowedPrivateData);
      } catch (error) {
        setName(null);
      } finally {
        setConnecting(false);
        isConnecting.current = false;
      }
    })();
  }, [isConnecting, connected, autoConnect, adapter, readyState, setName]);
  const connect = (0, import_react3.useCallback)(async () => {
    if (isConnecting.current || isDisconnecting.current || connected)
      return;
    if (!adapter)
      throw handleError(new WalletNotSelectedError());
    if (!(readyState === WalletReadyState.Installed || readyState === WalletReadyState.Loadable)) {
      setName(null);
      if (typeof window !== "undefined") {
        window.open(adapter.url, "_blank");
      }
      throw handleError(new WalletNotReadyError());
    }
    isConnecting.current = true;
    setConnecting(true);
    try {
      await adapter.connect(privateDataPermission, network, allowedPrivateData);
    } catch (error) {
      setName(null);
      throw error;
    } finally {
      setConnecting(false);
      isConnecting.current = false;
    }
  }, [
    isConnecting,
    isDisconnecting,
    connected,
    adapter,
    readyState,
    handleError,
    setName
  ]);
  const disconnect = (0, import_react3.useCallback)(async () => {
    if (isDisconnecting.current)
      return;
    if (!adapter)
      return setName(null);
    isDisconnecting.current = true;
    setDisconnecting(true);
    try {
      await adapter.disconnect();
    } catch (error) {
      setName(null);
      throw error;
    } finally {
      setDisconnecting(false);
      isDisconnecting.current = false;
    }
  }, [isDisconnecting, adapter, setName]);
  const requestTransaction = (0, import_react3.useMemo)(() => adapter && "requestTransaction" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestTransaction(transaction);
  } : void 0, [adapter, handleError, connected]);
  const requestAssets = (0, import_react3.useMemo)(() => adapter && "requestAssets" in adapter ? async () => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestAssets();
  } : void 0, [adapter, handleError, connected]);
  const requestPrivateNotes = (0, import_react3.useMemo)(() => adapter && "requestPrivateNotes" in adapter ? async (noteFilterType, noteIds) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestPrivateNotes(noteFilterType, noteIds);
  } : void 0, [adapter, handleError, connected]);
  const signBytes = (0, import_react3.useMemo)(() => adapter && "signBytes" in adapter ? async (message, kind) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.signBytes(message, kind);
  } : void 0, [adapter, handleError, connected]);
  const importPrivateNote = (0, import_react3.useMemo)(() => adapter && "importPrivateNote" in adapter ? async (note) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.importPrivateNote(note);
  } : void 0, [adapter, handleError, connected]);
  const requestConsumableNotes = (0, import_react3.useMemo)(() => adapter && "requestConsumableNotes" in adapter ? async () => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestConsumableNotes();
  } : void 0, [adapter, handleError, connected]);
  const waitForTransaction = (0, import_react3.useMemo)(() => adapter && "waitForTransaction" in adapter ? async (txId, timeout) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.waitForTransaction(txId, timeout);
  } : void 0, [adapter, handleError, connected]);
  const requestSend = (0, import_react3.useMemo)(() => adapter && "requestSend" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestSend(transaction);
  } : void 0, [adapter, handleError, connected]);
  const requestConsume = (0, import_react3.useMemo)(() => adapter && "requestConsume" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestConsume(transaction);
  } : void 0, [adapter, handleError, connected]);
  return (0, import_jsx_runtime.jsx)(WalletContext.Provider, { value: {
    autoConnect,
    wallets,
    wallet,
    address,
    publicKey,
    connected,
    connecting,
    disconnecting,
    select: setName,
    connect,
    disconnect,
    requestTransaction,
    requestAssets,
    requestPrivateNotes,
    signBytes,
    importPrivateNote,
    requestConsumableNotes,
    waitForTransaction,
    requestSend,
    requestConsume
  }, children });
};

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/MidenFiSignerProvider.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);
import { SignerContext } from "@miden-sdk/react";

// node_modules/@miden-sdk/miden-wallet-adapter-miden/dist/adapter.js
import { Note } from "@miden-sdk/miden-sdk";
var MidenWalletName = "Miden Wallet";
var MidenWalletAdapter = class extends BaseMessageSignerWalletAdapter {
  name = MidenWalletName;
  url = "https://chromewebstore.google.com/detail/miden-wallet/ablmompanofnodfdkgchkpmphailefpb";
  icon = "data:image/png;base64,";
  supportedTransactionVersions = null;
  _connecting;
  _wallet;
  _address;
  _publicKey;
  _privateDataPermission;
  _readyState = typeof window === "undefined" || typeof document === "undefined" ? WalletReadyState.Unsupported : WalletReadyState.NotDetected;
  constructor({ appName = "sample" } = {}) {
    super();
    this._connecting = false;
    this._wallet = null;
    this._address = null;
    this._publicKey = null;
    this._privateDataPermission = PrivateDataPermission.UponRequest;
    if (this._readyState !== WalletReadyState.Unsupported) {
      scopePollingDetectionStrategy(() => {
        if (window?.midenWallet || window?.miden) {
          this._readyState = WalletReadyState.Installed;
          this.emit("readyStateChange", this._readyState);
          return true;
        }
        return false;
      });
    }
  }
  get address() {
    return this._address;
  }
  get publicKey() {
    return this._publicKey;
  }
  get privateDataPermission() {
    return this._privateDataPermission;
  }
  get connecting() {
    return this._connecting;
  }
  get readyState() {
    return this._readyState;
  }
  set readyState(readyState) {
    this._readyState = readyState;
  }
  async requestSend(transaction) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.requestSend(transaction);
        return result.transactionId;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async requestConsume(transaction) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.requestConsume(transaction);
        return result.transactionId;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async requestTransaction(transaction) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.requestTransaction(transaction);
        return result.transactionId;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async requestAssets() {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.requestAssets();
        return result.assets;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async requestPrivateNotes(noteFilterType, noteIds) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.requestPrivateNotes(noteFilterType, noteIds);
        return result.privateNotes;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async signBytes(message, kind) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.signBytes(message, kind);
        return result.signature;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async importPrivateNote(note) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      const result = await wallet.importPrivateNote(note);
      return result.noteId;
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async requestConsumableNotes() {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      const result = await wallet.requestConsumableNotes();
      return result.consumableNotes;
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async waitForTransaction(txId, timeout) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      const result = await wallet.waitForTransaction(txId, timeout);
      if ("errorMessage" in result) {
        throw new WalletTransactionError(result.errorMessage);
      }
      const notes = result.outputNotes.map((serNote) => {
        const u8Arr = b64ToU8(serNote);
        return Note.deserialize(u8Arr);
      });
      return {
        txHash: result.txHash,
        outputNotes: notes
      };
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async createAccount(params) {
    try {
      const wallet = this._wallet;
      if (!wallet || !this.address)
        throw new WalletNotConnectedError();
      try {
        const result = await wallet.createAccount(params);
        return result.accountId;
      } catch (error) {
        throw new WalletTransactionError(error?.message, error);
      }
    } catch (error) {
      this.emit("error", error);
      throw error;
    }
  }
  async connect(privateDataPermission, network, allowedPrivateData) {
    try {
      if (this.connected || this.connecting)
        return;
      if (this._readyState !== WalletReadyState.Installed)
        throw new WalletNotReadyError();
      this._connecting = true;
      const wallet = window.midenWallet || window.miden;
      try {
        await wallet.connect(privateDataPermission, network, allowedPrivateData);
        if (!wallet.address) {
          throw new WalletConnectionError();
        }
        this._address = wallet.address;
        this._publicKey = wallet.publicKey;
      } catch (error) {
        throw new WalletConnectionError(error?.message, error);
      }
      this._wallet = wallet;
      this._privateDataPermission = privateDataPermission;
      this.emit("connect", this._address);
    } catch (error) {
      this.emit("error", error);
      throw error;
    } finally {
      this._connecting = false;
    }
  }
  async disconnect() {
    const wallet = this._wallet;
    if (wallet) {
      this._wallet = null;
      this._address = null;
      this._publicKey = null;
      try {
        await wallet.disconnect();
      } catch (error) {
        this.emit("error", new WalletDisconnectionError(error?.message, error));
      }
    }
    this.emit("disconnect");
  }
};

// node_modules/@miden-sdk/miden-wallet-adapter-react/dist/MidenFiSignerProvider.js
var WalletContext2 = (0, import_react4.createContext)({});
var initialState2 = {
  wallet: null,
  adapter: null,
  address: null,
  publicKey: null,
  connected: false
};
var MidenFiSignerProvider = ({ children, wallets: walletsProp, appName = "Miden DApp", network = WalletAdapterNetwork.Testnet, autoConnect = false, privateDataPermission = PrivateDataPermission.UponRequest, allowedPrivateData = AllowedPrivateData.None, onError, localStorageKey = "walletName", accountType = "RegularAccountImmutableCode", storageMode = "public", customComponents, importAccountId }) => {
  const adapters = (0, import_react4.useMemo)(() => walletsProp ?? [new MidenWalletAdapter({ appName })], [walletsProp, appName]);
  const [name, setName] = useLocalStorage(localStorageKey, null);
  const [{ wallet, adapter, address, publicKey, connected }, setState] = (0, import_react4.useState)(initialState2);
  const readyState = adapter?.readyState || WalletReadyState.Unsupported;
  const [connecting, setConnecting] = (0, import_react4.useState)(false);
  const [disconnecting, setDisconnecting] = (0, import_react4.useState)(false);
  const isConnecting = (0, import_react4.useRef)(false);
  const isDisconnecting = (0, import_react4.useRef)(false);
  const isUnloading = (0, import_react4.useRef)(false);
  const [wallets, setWallets] = (0, import_react4.useState)(() => adapters.map((adapter2) => ({
    adapter: adapter2,
    readyState: adapter2.readyState
  })));
  (0, import_react4.useEffect)(() => {
    setWallets((wallets2) => adapters.map((adapter2, index) => {
      const wallet2 = wallets2[index];
      return wallet2 && wallet2.adapter === adapter2 && wallet2.readyState === adapter2.readyState ? wallet2 : {
        adapter: adapter2,
        readyState: adapter2.readyState
      };
    }));
    function handleReadyStateChange(readyState2) {
      setWallets((prevWallets) => {
        const index = prevWallets.findIndex(({ adapter: adapter3 }) => adapter3 === this);
        if (index === -1)
          return prevWallets;
        const { adapter: adapter2 } = prevWallets[index];
        return [
          ...prevWallets.slice(0, index),
          { adapter: adapter2, readyState: readyState2 },
          ...prevWallets.slice(index + 1)
        ];
      });
    }
    adapters.forEach((adapter2) => adapter2.on("readyStateChange", handleReadyStateChange, adapter2));
    return () => adapters.forEach((adapter2) => adapter2.off("readyStateChange", handleReadyStateChange, adapter2));
  }, [adapters]);
  (0, import_react4.useEffect)(() => {
    const found = name && wallets.find(({ adapter: adapter2 }) => adapter2.name === name);
    if (found) {
      setState((prev) => {
        if (prev.wallet === found && prev.adapter === found.adapter && prev.connected === found.adapter.connected && prev.address === found.adapter.address && prev.publicKey === found.adapter.publicKey)
          return prev;
        return {
          wallet: found,
          adapter: found.adapter,
          connected: found.adapter.connected,
          address: found.adapter.address,
          publicKey: found.adapter.publicKey
        };
      });
    } else {
      setState((prev) => prev === initialState2 ? prev : initialState2);
    }
  }, [name, wallets]);
  (0, import_react4.useEffect)(() => {
    function listener() {
      isUnloading.current = true;
    }
    window.addEventListener("beforeunload", listener);
    return () => window.removeEventListener("beforeunload", listener);
  }, [isUnloading]);
  const handleConnect = (0, import_react4.useCallback)(() => {
    if (!adapter)
      return;
    setState((prev) => {
      if (prev.connected === adapter.connected && prev.address === adapter.address && prev.publicKey === adapter.publicKey)
        return prev;
      return {
        ...prev,
        connected: adapter.connected,
        address: adapter.address,
        publicKey: adapter.publicKey
      };
    });
  }, [adapter]);
  const handleDisconnect = (0, import_react4.useCallback)(() => {
    if (!isUnloading.current)
      setName(null);
  }, [isUnloading, setName]);
  const handleError = (0, import_react4.useCallback)((error) => {
    if (!isUnloading.current)
      (onError || console.error)(error);
    return error;
  }, [isUnloading, onError]);
  (0, import_react4.useEffect)(() => {
    if (adapter) {
      adapter.on("connect", handleConnect);
      adapter.on("disconnect", handleDisconnect);
      adapter.on("error", handleError);
      return () => {
        adapter.off("connect", handleConnect);
        adapter.off("disconnect", handleDisconnect);
        adapter.off("error", handleError);
      };
    }
  }, [adapter, handleConnect, handleDisconnect, handleError]);
  (0, import_react4.useEffect)(() => {
    return () => {
      adapter?.disconnect();
    };
  }, [adapter]);
  (0, import_react4.useEffect)(() => {
    if (!name && wallets.length === 1) {
      setName(wallets[0].adapter.name);
    }
  }, [name, wallets, setName]);
  (0, import_react4.useEffect)(() => {
    if (isConnecting.current || connected || !autoConnect || !adapter || !(readyState === WalletReadyState.Installed || readyState === WalletReadyState.Loadable))
      return;
    (async function() {
      isConnecting.current = true;
      setConnecting(true);
      try {
        await adapter.connect(privateDataPermission, network, allowedPrivateData);
      } catch (error) {
        setName(null);
      } finally {
        setConnecting(false);
        isConnecting.current = false;
      }
    })();
  }, [isConnecting, connected, autoConnect, adapter, readyState, setName, privateDataPermission, network, allowedPrivateData]);
  const connect = (0, import_react4.useCallback)(async () => {
    if (isConnecting.current || isDisconnecting.current || connected)
      return;
    if (!adapter)
      throw handleError(new WalletNotSelectedError());
    if (!(readyState === WalletReadyState.Installed || readyState === WalletReadyState.Loadable)) {
      setName(null);
      if (typeof window !== "undefined") {
        window.open(adapter.url, "_blank");
      }
      throw handleError(new WalletNotReadyError());
    }
    isConnecting.current = true;
    setConnecting(true);
    try {
      await adapter.connect(privateDataPermission, network, allowedPrivateData);
    } catch (error) {
      setName(null);
      throw error;
    } finally {
      setConnecting(false);
      isConnecting.current = false;
    }
  }, [
    isConnecting,
    isDisconnecting,
    connected,
    adapter,
    readyState,
    handleError,
    setName,
    privateDataPermission,
    network,
    allowedPrivateData
  ]);
  const disconnect = (0, import_react4.useCallback)(async () => {
    if (isDisconnecting.current)
      return;
    if (!adapter)
      return setName(null);
    isDisconnecting.current = true;
    setDisconnecting(true);
    try {
      await adapter.disconnect();
    } catch (error) {
      setName(null);
      throw error;
    } finally {
      setDisconnecting(false);
      isDisconnecting.current = false;
    }
  }, [isDisconnecting, adapter, setName]);
  const requestTransaction = (0, import_react4.useMemo)(() => adapter && "requestTransaction" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestTransaction(transaction);
  } : void 0, [adapter, handleError, connected]);
  const requestAssets = (0, import_react4.useMemo)(() => adapter && "requestAssets" in adapter ? async () => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestAssets();
  } : void 0, [adapter, handleError, connected]);
  const requestPrivateNotes = (0, import_react4.useMemo)(() => adapter && "requestPrivateNotes" in adapter ? async (noteFilterType, noteIds) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestPrivateNotes(noteFilterType, noteIds);
  } : void 0, [adapter, handleError, connected]);
  const signBytes = (0, import_react4.useMemo)(() => adapter && "signBytes" in adapter ? async (message, kind) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.signBytes(message, kind);
  } : void 0, [adapter, handleError, connected]);
  const importPrivateNote = (0, import_react4.useMemo)(() => adapter && "importPrivateNote" in adapter ? async (note) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.importPrivateNote(note);
  } : void 0, [adapter, handleError, connected]);
  const requestConsumableNotes = (0, import_react4.useMemo)(() => adapter && "requestConsumableNotes" in adapter ? async () => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestConsumableNotes();
  } : void 0, [adapter, handleError, connected]);
  const waitForTransaction = (0, import_react4.useMemo)(() => adapter && "waitForTransaction" in adapter ? async (txId, timeout) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.waitForTransaction(txId, timeout);
  } : void 0, [adapter, handleError, connected]);
  const requestSend = (0, import_react4.useMemo)(() => adapter && "requestSend" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestSend(transaction);
  } : void 0, [adapter, handleError, connected]);
  const requestConsume = (0, import_react4.useMemo)(() => adapter && "requestConsume" in adapter ? async (transaction) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.requestConsume(transaction);
  } : void 0, [adapter, handleError, connected]);
  const createAccount = (0, import_react4.useMemo)(() => adapter && "createAccount" in adapter ? async (params) => {
    if (!connected)
      throw handleError(new WalletNotConnectedError());
    return await adapter.createAccount(params);
  } : void 0, [adapter, handleError, connected]);
  const signBytesRef = (0, import_react4.useRef)(signBytes);
  (0, import_react4.useEffect)(() => {
    signBytesRef.current = signBytes;
  }, [signBytes]);
  const connectRef = (0, import_react4.useRef)(connect);
  (0, import_react4.useEffect)(() => {
    connectRef.current = connect;
  }, [connect]);
  const disconnectRef = (0, import_react4.useRef)(disconnect);
  (0, import_react4.useEffect)(() => {
    disconnectRef.current = disconnect;
  }, [disconnect]);
  const disconnectedCtx = (0, import_react4.useRef)({
    signCb: async () => {
      throw new Error("MidenFi wallet not connected");
    },
    accountConfig: null,
    storeName: "",
    name: "MidenFi",
    isConnected: false,
    connect: async () => {
      await connectRef.current();
    },
    disconnect: async () => {
      await disconnectRef.current();
    }
  });
  const connectedCtxRef = (0, import_react4.useRef)(null);
  const [signerContext, setSignerContext] = (0, import_react4.useState)(disconnectedCtx.current);
  (0, import_react4.useEffect)(() => {
    let cancelled = false;
    async function buildContext() {
      if (!connected || !publicKey || !address || !signBytesRef.current) {
        if (connectedCtxRef.current !== null) {
          connectedCtxRef.current = null;
          setSignerContext(disconnectedCtx.current);
        }
        return;
      }
      try {
        if (!cancelled) {
          const { AccountStorageMode } = await import("@miden-sdk/miden-sdk");
          const signCb = async (_, signingInputs) => {
            const result = await signBytesRef.current(signingInputs, "signingInputs");
            return result;
          };
          const resolvedStorageMode = AccountStorageMode.tryFromStr(storageMode);
          const ctx = {
            signCb,
            accountConfig: {
              publicKeyCommitment: publicKey,
              accountType,
              storageMode: resolvedStorageMode,
              ...customComponents?.length ? { customComponents } : {},
              importAccountId: importAccountId ?? address
            },
            storeName: `midenfi_${address}`,
            name: "MidenFi",
            isConnected: true,
            connect: connectRef.current,
            disconnect: disconnectRef.current
          };
          connectedCtxRef.current = ctx;
          setSignerContext(ctx);
        }
      } catch (error) {
        console.error("Failed to build MidenFi signer context:", error);
        if (!cancelled) {
          connectedCtxRef.current = null;
          setSignerContext(disconnectedCtx.current);
        }
      }
    }
    buildContext();
    return () => {
      cancelled = true;
    };
  }, [connected, publicKey, address, accountType, storageMode, customComponents, importAccountId]);
  const walletContextValue = (0, import_react4.useMemo)(() => ({
    autoConnect,
    wallets,
    wallet,
    address,
    publicKey,
    connected,
    connecting,
    disconnecting,
    select: setName,
    connect,
    disconnect,
    requestTransaction,
    requestAssets,
    requestPrivateNotes,
    signBytes,
    importPrivateNote,
    requestConsumableNotes,
    waitForTransaction,
    requestSend,
    requestConsume,
    createAccount
  }), [
    autoConnect,
    wallets,
    wallet,
    address,
    publicKey,
    connected,
    connecting,
    disconnecting,
    setName,
    connect,
    disconnect,
    requestTransaction,
    requestAssets,
    requestPrivateNotes,
    signBytes,
    importPrivateNote,
    requestConsumableNotes,
    waitForTransaction,
    requestSend,
    requestConsume,
    createAccount
  ]);
  return (0, import_jsx_runtime2.jsx)(WalletContext2.Provider, { value: walletContextValue, children: (0, import_jsx_runtime2.jsx)(SignerContext.Provider, { value: signerContext, children }) });
};
function useMidenFiWallet() {
  const context = (0, import_react4.useContext)(WalletContext2);
  if (!context || Object.keys(context).length === 0) {
    throw new Error("useMidenFiWallet must be used within MidenFiSignerProvider");
  }
  return context;
}
export {
  MidenFiSignerProvider,
  WalletContext2 as WalletContext,
  WalletProvider,
  useLocalStorage,
  useMidenFiWallet,
  useWallet
};
//# sourceMappingURL=@miden-sdk_miden-wallet-adapter-react.js.map
